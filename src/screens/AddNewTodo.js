import { SafeAreaView } from "react-native";
import styles from "../styles/screens/AddNewTodo";

const AddNewTodo = ()=>{
    return(
        <SafeAreaView style={styles.container}>
            <Text>Apenas um teste 2 </Text>
        </SafeAreaView>
    );
}

export default AddNewTodo;
import { SafeAreaView, Text } from "react-native";
import styles from "../styles/screens/AddNewElement";

const AddNewElement = ()=>{
    return(
        <SafeAreaView style={styles.container}>
            <Text>Apenas um teste 2 Auiuuuuuuuu</Text>
        </SafeAreaView>
    );
}

export default AddNewElement;